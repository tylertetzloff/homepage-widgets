# Copy to /etc/homepage-widgets/mkv.env — do not commit the real file.
# Comma-separated dest roots are allowed.
MKV_INBOX=/mnt/rips
MKV_OUT=/mnt/movies
MAKEMKVCON=/usr/bin/makemkvcon
MKV_MINLENGTH=120
BIND=0.0.0.0
PORT=8794
